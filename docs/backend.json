{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Represents a user's basic profile information.",
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "createdAt": {
            "type": "string",
            "format": "date-time",
            "description": "Timestamp of when the user account was created."
        }
      },
      "required": ["email", "createdAt"]
    },
    "ProviderProfile": {
      "title": "Provider Profile",
      "description": "Represents the professional profile of a service provider.",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The UID of the user who submitted this profile."
        },
        "name": {
          "type": "string",
          "description": "The name of the service provider or business."
        },
        "category": {
          "type": "string",
          "description": "The category of service offered."
        },
        "tagline": {
          "type": "string",
          "description": "A short, catchy tagline for the provider."
        },
        "location": {
          "type": "string",
          "description": "The physical location or service area."
        },
        "phone": {
          "type": "string",
          "description": "Contact phone number."
        },
        "whatsapp": {
          "type": "string",
          "description": "Contact WhatsApp number (international format without symbols)."
        },
        "email": {
          "type": "string",
          "description": "Contact email address.",
          "format": "email"
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the provider and their services."
        },
        "services": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "A list of specific services offered."
        },
        "imageUrl": {
          "type": "string",
          "description": "URL of the provider's profile image."
        }
      },
      "required": [
        "userId",
        "name",
        "category",
        "location"
      ]
    }
  },
  "auth": {
    "providers": [
      "email-password"
    ]
  },
  "firestore": {
    "/users/{userId}": {
        "schema": {
            "$ref": "#/backend/entities/UserProfile"
        },
        "description": "Stores user profile data, keyed by their auth UID."
    },
    "/providers/{providerId}": {
      "schema": {
        "$ref": "#/backend/entities/ProviderProfile"
      },
      "description": "Stores the public profile for a service provider. The 'providerId' is a unique identifier."
    }
  }
}
